# logos
Logos is the AI reasoning and retrieval engine. It handles document parsing, chunking, embedding generation, vector search, and retrieval-augmented generation (RAG) for chat and question answering over user documents
